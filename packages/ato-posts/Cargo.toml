[package]
name = "ato-posts"
version = "0.1.0"
edition = "2021"
description = "Shared activity-feed types for ATO desktop + CLI. Pure type defs, no I/O."
license = "MIT"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
